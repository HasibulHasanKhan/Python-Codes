import random

options = ("rock", "paper", "scissors")
cards = ["2", "4", "7", "12", "A", "B", "C", "D", "E"]

# number = random.randint(1, 20)
# number = random.random()
# option = random.choice(options)
random.shuffle(cards)

print(cards)